# The backend uses a local SQLite file by default (api/shelfswap.db), so there is
# nothing to configure for local development. When you deploy (a workshop job),
# the host injects its own values; copy this file to .env if you add settings.
