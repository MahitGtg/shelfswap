# Where the React app looks for the API. Locally the default is fine, so you only
# need this file once you deploy (a workshop job) and the backend lives elsewhere.
VITE_API_URL=http://127.0.0.1:8000
